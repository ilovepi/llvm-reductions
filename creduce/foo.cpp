#define a(e, ab) e->ac = e->ac ?: ab
#define b(e, ab) return a(e, ab)
struct A {
  long d;
  const char *ac;
};
bool g();
char a;
bool f;
static bool h(unsigned *i, bool p2) {
  A *e = 0;
  if (p2)
    ;
  char b;
  do {
    if (g())
      if (b) {
        if (f)
          b(e, "");
        if (a)
          b(e, "");
      }
    b = 7;
  } while (a);
  *i = 5;
  return true;
}
bool fn3(bool i) {
  unsigned c(h(&c, i));
  return true;
}
